class DriverProfileMetrics {
  DriverProfileMetrics._();

  /// The hero avatar. 72 rather than the old 88: the identity block is a row
  /// now — face beside name — not a centred portrait, so the avatar is sized
  /// against the three lines of type next to it instead of against the card.
  static const double avatarSize = 72;
}
