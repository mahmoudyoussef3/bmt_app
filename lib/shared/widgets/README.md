# Shared Widgets

Future home for widgets reused by two or more products.

During Phase 0, the active implementation remains in `lib/core/widgets` so the
UI prototype keeps working. Move widgets here only when reuse has been verified.

