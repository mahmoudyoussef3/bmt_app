# Shared Common

Future home for product-agnostic common types such as failures, result wrappers,
validators, and small value objects.

Do not place product business entities here unless they are genuinely shared
contracts across all products.

