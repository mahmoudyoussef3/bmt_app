# Shared Theme

Future home for the shared design system.

During Phase 0, the active implementation remains in `lib/core/theme` so the UI
prototype keeps working. Move theme files here only during the approved shared
extraction phase.

