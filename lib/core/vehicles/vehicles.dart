/// Vehicle types and their cabin seat layouts — shared by the Owner Dashboard
/// (which generates and persists seat configurations) and the Client App
/// (which draws the seat map a rider books from).
library;

export 'seat_layout_blueprint.dart';
export 'vehicle_seat_layouts.dart';
export 'vehicle_type.dart';
