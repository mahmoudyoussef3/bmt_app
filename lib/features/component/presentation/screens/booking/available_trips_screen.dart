import 'package:flutter/material.dart';
import 'package:bmt_app/features/component/presentation/screens/booking/vehicle_listing_screen.dart';

/// Legacy route — shows the vehicle listing comparison screen.
class AvailableTripsScreen extends StatelessWidget {
  const AvailableTripsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const VehicleListingScreen();
  }
}
